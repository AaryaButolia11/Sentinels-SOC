# Core API
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12.0
python-dotenv>=1.0

# Data / DB
sqlalchemy>=2.0
pydantic>=2.0

# ML
scikit-learn>=1.4
pandas>=2.2
numpy>=1.26
lightgbm>=4.3
imbalanced-learn>=0.12
shap>=0.45

# Dataset generation
# (Faker is referenced in the README but generate_logs.py as shipped
# only needs stdlib + the above -- listed here in case you extend it)
Faker>=24.0

# Tests
pytest>=8.0
httpx>=0.27
psycopg2-binary